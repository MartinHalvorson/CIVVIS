<!-- Notes the heuristic gene ranking carries verbatim, under its table. Edit here, not in HEURISTIC_GENE_RANKING.md (generated). -->

**Direct follow-up.** This is a ranking screen, not a promotion queue. The subsequent [P9 direct confirmation](docs/eval/2026-08-21-current-genome-settler-guard-direct-confirmation.md) held every other deployment gene fixed and flipped only `settler-guard-holds` across 300 maps / 1,800 treated-seat pairs. It measured exactly **+0.0 pp** on wins and score share; the flag remains unresolved and off. Its +13 row below is retained as historical p7 screen output, not a current recommendation.

**Both regimes are re-priced together.** A gene's price is not a constant of nature — `wide-map-capacity` measured −3.4 pp in the all-six native regime and +19.2 pp under `domination,score` from the same code — so a cycle that refreshes only the native column leaves the war column quoting older code. This cycle therefore ran the two screens as a pair on disjoint seed windows: a 6p all-six native batch (seeds 100000000..) and a 4p `domination,score` war batch (seeds 101000000..), same binary, same 60x38 pangaea/online/250-turn profile, both foldover against the best-genome baseline with every major seat carrying its own genome. Read the *Last 10k* column and the *War* column as one reading of one commit, not as two cycles.
